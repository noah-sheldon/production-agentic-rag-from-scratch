# Module 00 Project — Your environment is ready

**Goal:** every check in `check_setup.py` passes, the repo is cloned, and the
terminal tutor loads. That's the whole module — the gate to everything else.

## Acceptance criteria

1. `python3 modules/00-setup-and-how-to-learn/check_setup.py` → ALL PASS.
2. The tutor skill loads: `npx skills add noah-sheldon/production-agentic-rag-from-scratch`
   then `/start-learning` starts the interview.
3. You can run your first lesson build:
   `python3 modules/01-production-infrastructure/lessons/01-what-is-a-container/code/build.py`.
4. You can explain, in your own words: build-first, the three gates, HITL.

## Done means

All four criteria pass and a human confirms you can explain the course rules
from memory (the Module 00 quiz, human-reviewed).
