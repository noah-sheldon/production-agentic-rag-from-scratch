# Python

Use the standard library first.

- A tool is a function with a good docstring.
- A tool schema has three parts: name, description, parameters.
- The description is the instruction manual — the model never sees the code.
- Test every tool by hand before the model ever calls it.
